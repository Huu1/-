<view class="container">
    <image class="icon" bind:tap="onLeft" src='{{latest?disLeftImg:leftImg}}' />
    <text class="title">{{title}}</text>
    <image class="icon" bind:tap="onRight" src='{{first?disRightImg:rightImg}}' />
</view>